<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="cron_loan_update_status" model="ir.cron">
        <field name="name">Loan: cập nhật quá hạn + tuổi nợ</field>
        <field name="model_id" ref="model_re_loan_note"/>
        <field name="state">code</field>
        <field name="code">model._cron_update_loan_status()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall"
               eval="(datetime.now() + timedelta(days=1)).strftime('%Y-%m-%d 23:00:00')"/>
        <field name="active" eval="True"/>
    </record>

    <record id="cron_loan_maturity_reminder" model="ir.cron">
        <field name="name">Loan: nhắc đáo hạn khế ước</field>
        <field name="model_id" ref="model_re_loan_note"/>
        <field name="state">code</field>
        <field name="code">model._cron_maturity_reminder()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall"
               eval="(datetime.now() + timedelta(days=1)).strftime('%Y-%m-%d 07:00:00')"/>
        <field name="active" eval="True"/>
    </record>
</odoo>
