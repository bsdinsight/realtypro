<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        DEPRECATED 2026-07-03 — cấu trúc menu (3 group HĐ tín dụng /
        Khế ước / Trích thu & Thanh toán) đã chuyển vào XML nguồn:
        re_loan/views/menu_root.xml. Ownership xmlid 3 group đã
        transfer sang module re_loan (UPDATE ir_model_data) nên file
        này không còn record nào — upgrade module không xoá gì.

        Module giữ lại (installed trên các DB cũ) như shell rỗng;
        KHÔNG thêm record mới vào đây.
    -->
</odoo>
